NWB-Vending.com — WHAT ACTUALLY CHANGED (June 15, 2026)
========================================================

YOU DID NOT CHANGE ANYTHING. HERE IS WHAT HAPPENED:

OLD PIPE (worked for months):
  edit → git push main → GitHub Pages rebuilds → NWB-Vending.com updated
  Every edit. Every push. Same pipe.

NEW PIPE (started today):
  Cloudflare's GitHub bot linked your repo and added wrangler.jsonc
  (commit 065c563, author: cloudflare-workers-and-pages[bot])

  Now:
  git push main  →  updates kelly-nwb.github.io/nwb-vending ONLY
  NWB-Vending.com  →  reads Cloudflare Workers (separate deploy)

  That is why preview and .com can show different text on the same day.

WHY THE BOT FIRED:
  Your domain DNS is on Cloudflare (normal). At some point the repo got
  connected in Cloudflare Workers & Pages (dashboard or auto-config).
  The bot commits wrangler.jsonc so Cloudflare serves the site — not GitHub.

KELLY'S JOB: push28 when ready. Nothing else.

AGENT'S JOB (every push28): see PUSH28-AGENT.md
  push → verify BOTH urls → wrangler deploy if .com stale → verify again

DEPLOY (July 2026 fix):
  Removed broken GitHub Action "Deploy NWB-Vending.com" (no secrets → failure email every push).
  NWB-Vending.com updates via Cloudflare Workers Git connection (wrangler.jsonc).
  Agent verifies both URLs after push28 (node scripts/verify-both-urls.mjs).

PREVIEW (always github after push):
  https://kelly-nwb.github.io/nwb-vending/

LIVE BRAND DOMAIN:
  https://NWB-Vending.com/